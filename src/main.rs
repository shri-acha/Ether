#![feature(assert_matches)]

mod error;
mod lexer;
mod codegen;
mod parser;
mod symbol_table;
mod tests;

mod semantic_analyzer;
mod type_checker;

use lexer::{TokenType, Tokenizer};
use parser::Parser;
fn main() {

    

}
